const Equipment = require('../Equipment');
class SteelSword extends Equipment {
    constructor(){ super("Espada de Aço", "Uma espada mais forte de aço", "arma", 12); }
}
module.exports = SteelSword;
